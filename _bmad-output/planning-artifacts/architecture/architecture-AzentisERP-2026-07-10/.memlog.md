---
scope: AzentisERP platform: multi-tenant provisioning, branding, and module-toggling layer built on forked ERPNext + stock Frappe Framework
purpose: build-substrate
altitude: feature
updated: 2026-07-10T20:29
---

- (version) Web-verified: ERPNext/Frappe version-15 requires Python 3.11+, Node 18+, MariaDB 10.3.x minimum; version-16 exists (newer) requiring Python 3.14/Node 24 -- decision: stay on version-15 for MVP (stability, docs maturity), revisit v16 post-MVP
- (version) Web-verified: Frappe's native offsite backup mechanism (s3_backup_enabled in site_config.json / push_backup script) defaults to AWS S3 ap-south-1 (Mumbai) region -- aligns with India data-residency decision (PRD 10.4)
- (decision) AD-1: All customization lives only in the Branding App (our_brand); dependency direction is our_brand -> Frappe/ERPNext APIs only, never reverse. Prevents future direct edits to forked source breaking upstream mergeability.
- (decision) AD-2: Tenant Settings (one DocType, one record per site) is sole source of branding+enabled_modules truth, read only through one cached accessor. Prevents state duplicated/drifted across CSS files, config.json, DocType.
- (decision) AD-3: Branding injection is live/dynamic (inline CSS custom properties from a page-load hook), never a compiled per-tenant static asset file. Prevents violating FR-8's no-restart requirement.
- (decision) AD-4: Module toggling (enabled_modules) is a navigation/sidebar-visibility convenience only at MVP, NOT a permission/security boundary -- does not restrict direct DocType/API access. Acceptable since all users within one tenant are that tenant's own trusted staff. Full RBAC-level enforcement deferred.
- (decision) AD-5: Module Preset is its own DocType (data), not hardcoded Python logic. Applying a preset copies its module list into the new Tenant Settings record (snapshot, not live FK). Prevents code changes being required to add a 5th preset.
- (decision) AD-6: Module dependency checks are warn-only (PRD 10.6), enforced through exactly one shared dependency-map + validation function called by both preset-apply and manual-toggle flows -- prevents duplicated/inconsistent dependency logic.
- (decision) AD-7: Provisioning is one idempotent, resumable step-pipeline (each step checks current state before acting) so a partial failure can always be safely re-run, never requiring manual cleanup.
- (decision) AD-8: Provisioning runs as a bench custom command executed directly on the production server (SSH), not a hosted/remote API, at MVP. Prevents building an unneeded auth+network-exposed onboarding service before volume justifies it.
- (decision) AD-9: Staging is one more site on the same production bench/server, not a separate server -- MVP budget is a single 4GB/2vCPU box. Used exclusively to verify upstream merges before fleet rollout (FR-16).
- (decision) AD-10: Offsite backups (FR-13) use Frappe's native S3-compatible backup mechanism targeting an S3-compatible bucket in ap-south-1 (Mumbai), consistent with India data residency. Exact vendor/bucket not contracted -- assumption, not binding to AWS specifically.
- (assumption) Paradigm named as two-part: Frappe Hook/Plugin Extension (runtime/app layer) + Idempotent CLI Pipeline (provisioning/orchestration layer) -- reflects the two structurally distinct concerns in this platform
- (event) Reviewer gate ran: rubric walker (pass-with-notes), tech-version verification (pass-with-notes), adversarial divergence attack (fail -> fixed). Reviews at reviews/review-*.md
- (decision) Fixed AD-2/AD-3 contradiction: both now point to frappe.get_cached_doc accessor, auto-invalidated on save -- single mechanism satisfies both 'one accessor' and 'live, no restart'
- (decision) Fixed site-identity vs custom_domain contradiction (rubric+adversarial finding): site name is now the tenant's canonical internal identifier assigned at provisioning; custom_domain bound later via Frappe domain-alias as new AD-11, matching FR-7 and UJ-1's DNS-not-ready edge case
- (decision) Fixed enabled_modules field-shape mismatch: standardized on Table MultiSelect (child rows, canonical Module Def names) everywhere -- spine ER diagram, consistency conventions, and addendum now agree
- (decision) Fixed AD-6 ambiguity: named the exact shared function path our_brand.module_rules.validate_dependencies() so preset-apply and manual-toggle flows cannot each build their own divergent copy
- (decision) Added AD-11: TLS issuance + custom-domain binding are separate, later, idempotent pipeline steps (not bundled into initial site creation, not a silent manual follow-up) -- closes ungoverned FR-12 gap found by both rubric and adversarial reviewers
- (decision) Fixed AD-10 factual error: ap-south-1 is OUR choice per India data-residency decision, not a Frappe default (version-verification reviewer found no such Frappe default exists)
- (version) Corrected stale versions per version-verification reviewer: MariaDB floor bumped 10.6+ -> 10.11+ (10.6 hit EOL 2026-07-06); Node.js noted as 20/22 LTS for actual deploy despite Frappe's stated 18+ floor (Node 18 EOL since 2025-04-30); v16-vs-v15 rationale tightened (v16 is GA, not immature -- risk is the combined Python/Node/framework jump, not v16 quality)
- (decision) Added two explicit Deferred items per rubric reviewer: welcome-email SMTP transport ownership, formal test/QA strategy -- previously silent, now tracked rather than unstated
- (event) Spine finalized: status set to final, all reviewer findings resolved
- (event) Added provisioning-pipeline flowchart (AD-7/AD-11 step order) to Structural Seed per user request to verify diagrams reflect the reviewer-driven fixes; ER diagram already carried the site_name/table_multiselect fix from the earlier reviewer pass
